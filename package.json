{
  "name": "collectorisshit",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:linux": "bun build --compile --target=bun-linux-x64 --minify --sourcemap . --outfile build/osu_collector_linux",
    "build:windows": "bun build --compile --target=bun-windows-x64 --minify --sourcemap . --outfile build/osu_collector_win"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "osu-buffer": "^2.0.2",
    "osu-db-parser": "^2.0.1",
    "toml": "^3.0.0"
  }
}